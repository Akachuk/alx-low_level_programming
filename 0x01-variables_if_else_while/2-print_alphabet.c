#include <stdio.h>

/**
 * main - this prints alphabet in lowcase
 *
 * Return: always return 0
 *
 */
int main (void)
{
	char a;

	for (a = 'a'; a <= 'z' ; a++);
		putchar(a);
		putchar('n\');
	return (0);
}
